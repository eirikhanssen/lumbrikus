$( document ).ready(function() {
    console.log( "lumbrikus.js ready!" );

    lightbox.option({
        'wrapAround': true,
        'albumLabel': 'Bilde %1 av %2',
        'fadeDuration': 400,
        'fadeImageDuration': 50,
        'resizeDuration': 100
      })
});